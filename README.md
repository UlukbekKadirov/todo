# This is my Todo App build with React + Firebase

## Available Scripts

In the project directory, you can run:

### Run

/* First, Install the needed packages */
npm install

/* Then start both Node and React */
npm start
